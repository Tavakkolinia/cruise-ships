const Ship = require('../cruise-ships.js');

describe ('', () => {
    it('returns the fact that people have boarded the ship at the port', () => {

        expect //people to have boarded the ship .tobe boarded;
    });
})